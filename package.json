{
  "devDependencies": {
    "@types/cors": "^2.8.13",
    "@types/node": "^20.5.7",
    "prettier": "^3.0.3",
    "husky": "^8.0.0"
  },
  "scripts": {
    "prepare": "husky install",
    "precommit": "npm run --prefix allthememesapp precommit && npm run --prefix server precommit"
  }
}
